# 在默认 HTTP server 块中添加 yunpindemo 域名的重定向
# 找到 default server 中的 "location / {" 之前，添加：

        # ===== yunpindemo HTTP 重定向 =====
        if ($host = 'yunpindemo.bijienetworks.com') {
                return 301 https://yunpindemo.bijienetworks.com$request_uri;
        }
